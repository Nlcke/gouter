import {Page} from 'components/Page';
import {View} from 'react-native';

/** @type {import("router").Screen<'LoginConfirmationStack'>} */
export const LoginConfirmationStack = ({children}) => {
  return <Page>{children}</Page>;
};
